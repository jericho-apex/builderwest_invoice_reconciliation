node_modules/
dist/
coverage/

# Planning docs kept locally, not tracked in the repo
docs/

# Local SQLite database + backups (never committed; on Render this lives on the persistent disk)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Secrets - never commit real credentials
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log
